// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               unknown
// source: agentanycast/v1/agent_card.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "agentanycast.v1";

/**
 * AgentCard is the A2A-compatible capability descriptor for an agent node.
 * Standard A2A fields are preserved; P2P extension fields are in the
 * `p2p_extension` submessage.
 */
export interface AgentCard {
  /** Standard A2A fields */
  name: string;
  description: string;
  version: string;
  /** e.g. "a2a/0.3" */
  protocolVersion: string;
  skills: Skill[];
  /** AgentAnycast P2P extension */
  p2pExtension?: P2PExtension | undefined;
}

/** Skill describes a single capability the agent exposes. */
export interface Skill {
  id: string;
  description: string;
  /** Optional JSON Schema describing the expected input for this skill. */
  inputSchema: string;
  /** Optional JSON Schema describing the output of this skill. */
  outputSchema: string;
}

/**
 * P2PExtension contains AgentAnycast-specific addressing information
 * appended to the standard A2A Agent Card.
 */
export interface P2PExtension {
  peerId: string;
  /** e.g. ["tcp", "quic"] */
  supportedTransports: string[];
  relayAddresses: string[];
  /**
   * Optional W3C DID (did:key method) derived from the node's Ed25519 public key.
   * Populated automatically by the daemon. Enables interoperability with
   * DID-based ecosystems such as ANP.
   */
  didKey: string;
  /** Optional did:web identifier for web-based DID resolution. */
  didWeb: string;
  /** Optional did:dns domain for DNS-based DID resolution. */
  didDns: string;
  /** Optional JSON-encoded Verifiable Credentials. */
  verifiableCredentials: string[];
}

function createBaseAgentCard(): AgentCard {
  return { name: "", description: "", version: "", protocolVersion: "", skills: [], p2pExtension: undefined };
}

export const AgentCard: MessageFns<AgentCard> = {
  encode(message: AgentCard, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    if (message.protocolVersion !== "") {
      writer.uint32(34).string(message.protocolVersion);
    }
    for (const v of message.skills) {
      Skill.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.p2pExtension !== undefined) {
      P2PExtension.encode(message.p2pExtension, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AgentCard {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAgentCard();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.protocolVersion = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.skills.push(Skill.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.p2pExtension = P2PExtension.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AgentCard {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.String(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.String(object.protocol_version)
        : "",
      skills: globalThis.Array.isArray(object?.skills) ? object.skills.map((e: any) => Skill.fromJSON(e)) : [],
      p2pExtension: isSet(object.p2pExtension)
        ? P2PExtension.fromJSON(object.p2pExtension)
        : isSet(object.p2p_extension)
        ? P2PExtension.fromJSON(object.p2p_extension)
        : undefined,
    };
  },

  toJSON(message: AgentCard): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.protocolVersion !== "") {
      obj.protocolVersion = message.protocolVersion;
    }
    if (message.skills?.length) {
      obj.skills = message.skills.map((e) => Skill.toJSON(e));
    }
    if (message.p2pExtension !== undefined) {
      obj.p2pExtension = P2PExtension.toJSON(message.p2pExtension);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentCard>, I>>(base?: I): AgentCard {
    return AgentCard.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentCard>, I>>(object: I): AgentCard {
    const message = createBaseAgentCard();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.version = object.version ?? "";
    message.protocolVersion = object.protocolVersion ?? "";
    message.skills = object.skills?.map((e) => Skill.fromPartial(e)) || [];
    message.p2pExtension = (object.p2pExtension !== undefined && object.p2pExtension !== null)
      ? P2PExtension.fromPartial(object.p2pExtension)
      : undefined;
    return message;
  },
};

function createBaseSkill(): Skill {
  return { id: "", description: "", inputSchema: "", outputSchema: "" };
}

export const Skill: MessageFns<Skill> = {
  encode(message: Skill, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.inputSchema !== "") {
      writer.uint32(26).string(message.inputSchema);
    }
    if (message.outputSchema !== "") {
      writer.uint32(34).string(message.outputSchema);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Skill {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkill();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.inputSchema = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.outputSchema = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Skill {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      inputSchema: isSet(object.inputSchema)
        ? globalThis.String(object.inputSchema)
        : isSet(object.input_schema)
        ? globalThis.String(object.input_schema)
        : "",
      outputSchema: isSet(object.outputSchema)
        ? globalThis.String(object.outputSchema)
        : isSet(object.output_schema)
        ? globalThis.String(object.output_schema)
        : "",
    };
  },

  toJSON(message: Skill): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.inputSchema !== "") {
      obj.inputSchema = message.inputSchema;
    }
    if (message.outputSchema !== "") {
      obj.outputSchema = message.outputSchema;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Skill>, I>>(base?: I): Skill {
    return Skill.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Skill>, I>>(object: I): Skill {
    const message = createBaseSkill();
    message.id = object.id ?? "";
    message.description = object.description ?? "";
    message.inputSchema = object.inputSchema ?? "";
    message.outputSchema = object.outputSchema ?? "";
    return message;
  },
};

function createBaseP2PExtension(): P2PExtension {
  return {
    peerId: "",
    supportedTransports: [],
    relayAddresses: [],
    didKey: "",
    didWeb: "",
    didDns: "",
    verifiableCredentials: [],
  };
}

export const P2PExtension: MessageFns<P2PExtension> = {
  encode(message: P2PExtension, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.peerId !== "") {
      writer.uint32(10).string(message.peerId);
    }
    for (const v of message.supportedTransports) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.relayAddresses) {
      writer.uint32(26).string(v!);
    }
    if (message.didKey !== "") {
      writer.uint32(34).string(message.didKey);
    }
    if (message.didWeb !== "") {
      writer.uint32(42).string(message.didWeb);
    }
    if (message.didDns !== "") {
      writer.uint32(50).string(message.didDns);
    }
    for (const v of message.verifiableCredentials) {
      writer.uint32(58).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): P2PExtension {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseP2PExtension();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.peerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.supportedTransports.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.relayAddresses.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.didKey = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.didWeb = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.didDns = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.verifiableCredentials.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): P2PExtension {
    return {
      peerId: isSet(object.peerId)
        ? globalThis.String(object.peerId)
        : isSet(object.peer_id)
        ? globalThis.String(object.peer_id)
        : "",
      supportedTransports: globalThis.Array.isArray(object?.supportedTransports)
        ? object.supportedTransports.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.supported_transports)
        ? object.supported_transports.map((e: any) => globalThis.String(e))
        : [],
      relayAddresses: globalThis.Array.isArray(object?.relayAddresses)
        ? object.relayAddresses.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.relay_addresses)
        ? object.relay_addresses.map((e: any) => globalThis.String(e))
        : [],
      didKey: isSet(object.didKey)
        ? globalThis.String(object.didKey)
        : isSet(object.did_key)
        ? globalThis.String(object.did_key)
        : "",
      didWeb: isSet(object.didWeb)
        ? globalThis.String(object.didWeb)
        : isSet(object.did_web)
        ? globalThis.String(object.did_web)
        : "",
      didDns: isSet(object.didDns)
        ? globalThis.String(object.didDns)
        : isSet(object.did_dns)
        ? globalThis.String(object.did_dns)
        : "",
      verifiableCredentials: globalThis.Array.isArray(object?.verifiableCredentials)
        ? object.verifiableCredentials.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.verifiable_credentials)
        ? object.verifiable_credentials.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: P2PExtension): unknown {
    const obj: any = {};
    if (message.peerId !== "") {
      obj.peerId = message.peerId;
    }
    if (message.supportedTransports?.length) {
      obj.supportedTransports = message.supportedTransports;
    }
    if (message.relayAddresses?.length) {
      obj.relayAddresses = message.relayAddresses;
    }
    if (message.didKey !== "") {
      obj.didKey = message.didKey;
    }
    if (message.didWeb !== "") {
      obj.didWeb = message.didWeb;
    }
    if (message.didDns !== "") {
      obj.didDns = message.didDns;
    }
    if (message.verifiableCredentials?.length) {
      obj.verifiableCredentials = message.verifiableCredentials;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<P2PExtension>, I>>(base?: I): P2PExtension {
    return P2PExtension.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<P2PExtension>, I>>(object: I): P2PExtension {
    const message = createBaseP2PExtension();
    message.peerId = object.peerId ?? "";
    message.supportedTransports = object.supportedTransports?.map((e) => e) || [];
    message.relayAddresses = object.relayAddresses?.map((e) => e) || [];
    message.didKey = object.didKey ?? "";
    message.didWeb = object.didWeb ?? "";
    message.didDns = object.didDns ?? "";
    message.verifiableCredentials = object.verifiableCredentials?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
