// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               unknown
// source: agentanycast/v1/federation.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "../../google/protobuf/timestamp";
import { SkillInfo } from "./registry_service";

export const protobufPackage = "agentanycast.v1";

export interface SyncRegistrationsRequest {
  relayId: string;
  since?: Date | undefined;
  limit: number;
}

export interface SyncRegistrationsResponse {
  registrations: FederatedRegistration[];
  syncTimestamp?: Date | undefined;
}

export interface PushRegistrationsRequest {
  relayId: string;
  registrations: FederatedRegistration[];
}

export interface PushRegistrationsResponse {
  accepted: number;
  rejected: number;
}

export interface FederatedRegistration {
  peerId: string;
  originRelayId: string;
  skills: SkillInfo[];
  agentName: string;
  agentDescription: string;
  registeredAt?: Date | undefined;
  expiresAt?: Date | undefined;
  version: number;
}

function createBaseSyncRegistrationsRequest(): SyncRegistrationsRequest {
  return { relayId: "", since: undefined, limit: 0 };
}

export const SyncRegistrationsRequest: MessageFns<SyncRegistrationsRequest> = {
  encode(message: SyncRegistrationsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.relayId !== "") {
      writer.uint32(10).string(message.relayId);
    }
    if (message.since !== undefined) {
      Timestamp.encode(toTimestamp(message.since), writer.uint32(18).fork()).join();
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncRegistrationsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncRegistrationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.relayId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.since = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncRegistrationsRequest {
    return {
      relayId: isSet(object.relayId)
        ? globalThis.String(object.relayId)
        : isSet(object.relay_id)
        ? globalThis.String(object.relay_id)
        : "",
      since: isSet(object.since) ? fromJsonTimestamp(object.since) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: SyncRegistrationsRequest): unknown {
    const obj: any = {};
    if (message.relayId !== "") {
      obj.relayId = message.relayId;
    }
    if (message.since !== undefined) {
      obj.since = message.since.toISOString();
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SyncRegistrationsRequest>, I>>(base?: I): SyncRegistrationsRequest {
    return SyncRegistrationsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SyncRegistrationsRequest>, I>>(object: I): SyncRegistrationsRequest {
    const message = createBaseSyncRegistrationsRequest();
    message.relayId = object.relayId ?? "";
    message.since = object.since ?? undefined;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseSyncRegistrationsResponse(): SyncRegistrationsResponse {
  return { registrations: [], syncTimestamp: undefined };
}

export const SyncRegistrationsResponse: MessageFns<SyncRegistrationsResponse> = {
  encode(message: SyncRegistrationsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.registrations) {
      FederatedRegistration.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.syncTimestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.syncTimestamp), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncRegistrationsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncRegistrationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registrations.push(FederatedRegistration.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.syncTimestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncRegistrationsResponse {
    return {
      registrations: globalThis.Array.isArray(object?.registrations)
        ? object.registrations.map((e: any) => FederatedRegistration.fromJSON(e))
        : [],
      syncTimestamp: isSet(object.syncTimestamp)
        ? fromJsonTimestamp(object.syncTimestamp)
        : isSet(object.sync_timestamp)
        ? fromJsonTimestamp(object.sync_timestamp)
        : undefined,
    };
  },

  toJSON(message: SyncRegistrationsResponse): unknown {
    const obj: any = {};
    if (message.registrations?.length) {
      obj.registrations = message.registrations.map((e) => FederatedRegistration.toJSON(e));
    }
    if (message.syncTimestamp !== undefined) {
      obj.syncTimestamp = message.syncTimestamp.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SyncRegistrationsResponse>, I>>(base?: I): SyncRegistrationsResponse {
    return SyncRegistrationsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SyncRegistrationsResponse>, I>>(object: I): SyncRegistrationsResponse {
    const message = createBaseSyncRegistrationsResponse();
    message.registrations = object.registrations?.map((e) => FederatedRegistration.fromPartial(e)) || [];
    message.syncTimestamp = object.syncTimestamp ?? undefined;
    return message;
  },
};

function createBasePushRegistrationsRequest(): PushRegistrationsRequest {
  return { relayId: "", registrations: [] };
}

export const PushRegistrationsRequest: MessageFns<PushRegistrationsRequest> = {
  encode(message: PushRegistrationsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.relayId !== "") {
      writer.uint32(10).string(message.relayId);
    }
    for (const v of message.registrations) {
      FederatedRegistration.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushRegistrationsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushRegistrationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.relayId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.registrations.push(FederatedRegistration.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushRegistrationsRequest {
    return {
      relayId: isSet(object.relayId)
        ? globalThis.String(object.relayId)
        : isSet(object.relay_id)
        ? globalThis.String(object.relay_id)
        : "",
      registrations: globalThis.Array.isArray(object?.registrations)
        ? object.registrations.map((e: any) => FederatedRegistration.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PushRegistrationsRequest): unknown {
    const obj: any = {};
    if (message.relayId !== "") {
      obj.relayId = message.relayId;
    }
    if (message.registrations?.length) {
      obj.registrations = message.registrations.map((e) => FederatedRegistration.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushRegistrationsRequest>, I>>(base?: I): PushRegistrationsRequest {
    return PushRegistrationsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushRegistrationsRequest>, I>>(object: I): PushRegistrationsRequest {
    const message = createBasePushRegistrationsRequest();
    message.relayId = object.relayId ?? "";
    message.registrations = object.registrations?.map((e) => FederatedRegistration.fromPartial(e)) || [];
    return message;
  },
};

function createBasePushRegistrationsResponse(): PushRegistrationsResponse {
  return { accepted: 0, rejected: 0 };
}

export const PushRegistrationsResponse: MessageFns<PushRegistrationsResponse> = {
  encode(message: PushRegistrationsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== 0) {
      writer.uint32(8).int32(message.accepted);
    }
    if (message.rejected !== 0) {
      writer.uint32(16).int32(message.rejected);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushRegistrationsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushRegistrationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rejected = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushRegistrationsResponse {
    return {
      accepted: isSet(object.accepted) ? globalThis.Number(object.accepted) : 0,
      rejected: isSet(object.rejected) ? globalThis.Number(object.rejected) : 0,
    };
  },

  toJSON(message: PushRegistrationsResponse): unknown {
    const obj: any = {};
    if (message.accepted !== 0) {
      obj.accepted = Math.round(message.accepted);
    }
    if (message.rejected !== 0) {
      obj.rejected = Math.round(message.rejected);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PushRegistrationsResponse>, I>>(base?: I): PushRegistrationsResponse {
    return PushRegistrationsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PushRegistrationsResponse>, I>>(object: I): PushRegistrationsResponse {
    const message = createBasePushRegistrationsResponse();
    message.accepted = object.accepted ?? 0;
    message.rejected = object.rejected ?? 0;
    return message;
  },
};

function createBaseFederatedRegistration(): FederatedRegistration {
  return {
    peerId: "",
    originRelayId: "",
    skills: [],
    agentName: "",
    agentDescription: "",
    registeredAt: undefined,
    expiresAt: undefined,
    version: 0,
  };
}

export const FederatedRegistration: MessageFns<FederatedRegistration> = {
  encode(message: FederatedRegistration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.peerId !== "") {
      writer.uint32(10).string(message.peerId);
    }
    if (message.originRelayId !== "") {
      writer.uint32(18).string(message.originRelayId);
    }
    for (const v of message.skills) {
      SkillInfo.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.agentName !== "") {
      writer.uint32(34).string(message.agentName);
    }
    if (message.agentDescription !== "") {
      writer.uint32(42).string(message.agentDescription);
    }
    if (message.registeredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.registeredAt), writer.uint32(50).fork()).join();
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(58).fork()).join();
    }
    if (message.version !== 0) {
      writer.uint32(64).uint64(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FederatedRegistration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFederatedRegistration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.peerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.originRelayId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.skills.push(SkillInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.agentName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.agentDescription = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.registeredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.version = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FederatedRegistration {
    return {
      peerId: isSet(object.peerId)
        ? globalThis.String(object.peerId)
        : isSet(object.peer_id)
        ? globalThis.String(object.peer_id)
        : "",
      originRelayId: isSet(object.originRelayId)
        ? globalThis.String(object.originRelayId)
        : isSet(object.origin_relay_id)
        ? globalThis.String(object.origin_relay_id)
        : "",
      skills: globalThis.Array.isArray(object?.skills) ? object.skills.map((e: any) => SkillInfo.fromJSON(e)) : [],
      agentName: isSet(object.agentName)
        ? globalThis.String(object.agentName)
        : isSet(object.agent_name)
        ? globalThis.String(object.agent_name)
        : "",
      agentDescription: isSet(object.agentDescription)
        ? globalThis.String(object.agentDescription)
        : isSet(object.agent_description)
        ? globalThis.String(object.agent_description)
        : "",
      registeredAt: isSet(object.registeredAt)
        ? fromJsonTimestamp(object.registeredAt)
        : isSet(object.registered_at)
        ? fromJsonTimestamp(object.registered_at)
        : undefined,
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
    };
  },

  toJSON(message: FederatedRegistration): unknown {
    const obj: any = {};
    if (message.peerId !== "") {
      obj.peerId = message.peerId;
    }
    if (message.originRelayId !== "") {
      obj.originRelayId = message.originRelayId;
    }
    if (message.skills?.length) {
      obj.skills = message.skills.map((e) => SkillInfo.toJSON(e));
    }
    if (message.agentName !== "") {
      obj.agentName = message.agentName;
    }
    if (message.agentDescription !== "") {
      obj.agentDescription = message.agentDescription;
    }
    if (message.registeredAt !== undefined) {
      obj.registeredAt = message.registeredAt.toISOString();
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FederatedRegistration>, I>>(base?: I): FederatedRegistration {
    return FederatedRegistration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FederatedRegistration>, I>>(object: I): FederatedRegistration {
    const message = createBaseFederatedRegistration();
    message.peerId = object.peerId ?? "";
    message.originRelayId = object.originRelayId ?? "";
    message.skills = object.skills?.map((e) => SkillInfo.fromPartial(e)) || [];
    message.agentName = object.agentName ?? "";
    message.agentDescription = object.agentDescription ?? "";
    message.registeredAt = object.registeredAt ?? undefined;
    message.expiresAt = object.expiresAt ?? undefined;
    message.version = object.version ?? 0;
    return message;
  },
};

/** FederationService enables relay-to-relay skill registry synchronization. */
export type FederationServiceService = typeof FederationServiceService;
export const FederationServiceService = {
  /** SyncRegistrations pulls registration updates from a peer relay. */
  syncRegistrations: {
    path: "/agentanycast.v1.FederationService/SyncRegistrations" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SyncRegistrationsRequest): Buffer =>
      Buffer.from(SyncRegistrationsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SyncRegistrationsRequest => SyncRegistrationsRequest.decode(value),
    responseSerialize: (value: SyncRegistrationsResponse): Buffer =>
      Buffer.from(SyncRegistrationsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SyncRegistrationsResponse => SyncRegistrationsResponse.decode(value),
  },
  /** PushRegistrations sends local registration changes to a peer relay. */
  pushRegistrations: {
    path: "/agentanycast.v1.FederationService/PushRegistrations" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PushRegistrationsRequest): Buffer =>
      Buffer.from(PushRegistrationsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PushRegistrationsRequest => PushRegistrationsRequest.decode(value),
    responseSerialize: (value: PushRegistrationsResponse): Buffer =>
      Buffer.from(PushRegistrationsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PushRegistrationsResponse => PushRegistrationsResponse.decode(value),
  },
} as const;

export interface FederationServiceServer extends UntypedServiceImplementation {
  /** SyncRegistrations pulls registration updates from a peer relay. */
  syncRegistrations: handleUnaryCall<SyncRegistrationsRequest, SyncRegistrationsResponse>;
  /** PushRegistrations sends local registration changes to a peer relay. */
  pushRegistrations: handleUnaryCall<PushRegistrationsRequest, PushRegistrationsResponse>;
}

export interface FederationServiceClient extends Client {
  /** SyncRegistrations pulls registration updates from a peer relay. */
  syncRegistrations(
    request: SyncRegistrationsRequest,
    callback: (error: ServiceError | null, response: SyncRegistrationsResponse) => void,
  ): ClientUnaryCall;
  syncRegistrations(
    request: SyncRegistrationsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SyncRegistrationsResponse) => void,
  ): ClientUnaryCall;
  syncRegistrations(
    request: SyncRegistrationsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SyncRegistrationsResponse) => void,
  ): ClientUnaryCall;
  /** PushRegistrations sends local registration changes to a peer relay. */
  pushRegistrations(
    request: PushRegistrationsRequest,
    callback: (error: ServiceError | null, response: PushRegistrationsResponse) => void,
  ): ClientUnaryCall;
  pushRegistrations(
    request: PushRegistrationsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PushRegistrationsResponse) => void,
  ): ClientUnaryCall;
  pushRegistrations(
    request: PushRegistrationsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PushRegistrationsResponse) => void,
  ): ClientUnaryCall;
}

export const FederationServiceClient = makeGenericClientConstructor(
  FederationServiceService,
  "agentanycast.v1.FederationService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): FederationServiceClient;
  service: typeof FederationServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
